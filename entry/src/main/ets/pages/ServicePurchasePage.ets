@Entry
@Component
struct ServicePurchasePage {
  build() {
    Column() {
      Text('服务采购页面占位')
        .fontSize(20)
        .fontWeight(FontWeight.Medium);
    }
    .width('100%')
    .height('100%')
    .justifyContent(FlexAlign.Center)
    .alignItems(HorizontalAlign.Center);
  }
}
